using System;

namespace TcBlack
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");

            // Assignment: var1 := 1;
            // Call (method, fb, function)
            // Instruction  (RETURN, CONTINUE, EXIT, JMP, IF, CASE)
            // Loops (FOR, WHILE, REPEAT)
            // operators: MIN, MAX, ADR, functions and functionblocks?
        }
    }
}
